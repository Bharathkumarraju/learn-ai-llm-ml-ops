DAG stands for Directed Acyclic Graph. It is a workflow that defines a sequence of tasks with dependencies.

In Airflow, a DAG is a data pipeline that consists of tasks (jobs) connected in a way that ensures execution order.

